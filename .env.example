# Optional. When omitted, the app uses the built-in grounded planning fallback.
OPENAI_API_KEY=

# Optional. Override the model used by the /api/assistant endpoint.
OPENAI_MODEL=gpt-5.2

# Optional.
PORT=4173
